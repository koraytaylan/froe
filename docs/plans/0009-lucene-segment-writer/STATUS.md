# Plan 0009 — Lucene Segment Writer — ✅ Done

The roll-up row in [../STATUS.md](../STATUS.md) must stay in sync with this file. Task-level truth lives in [tasks/](tasks/) frontmatter; Makina's integration coordinator updates both layers.

- **Status:** ✅ Done.
- **Goal:** write Lucene 4.7.2 indexes in the `oakCodec` composition from typed, pre-tokenized documents, single segment, spilling against a declared budget, accepted by Lucene's own index checker and read back identically to Lucene's own output.
- **Root cause:** every earlier plan moves index data; none creates it, and creating it is what offline reindexing means for the Lucene indexes that dominate AEM.
- **Approach:** specify each file format from the vendored Lucene sources, closing with a go/no-go feasibility verdict as the last section of `docs/analysis/lucene-4-7-codec.md` on which the first implementation task is gated; implement the primitives first and pin them with vectors from the image's Lucene; build the formats bottom-up; assemble a segment; compare contents with a Lucene-built index through the judge.
- **Progress:** 13/13 tasks done; 0 blocked; 0 dropped.
- **Integration:** `planned`; run —; base `develop` @ `314b9c704fef73636d40f3e7ec5ff2c839aa1870` plus plans 0006–0008 merged; validation base —; mode —; final integration —.
- **Exceptions:** 0913 is `gated: true` on the maintainer's acceptance of the freeze, and is **left open** beside plans 0007 and 0008's own freeze tasks: a freeze is a maintainer's sign-off, not an implementation step. 0902 is `gated: true` on the maintainer's acceptance of the feasibility verdict; the gate was **opened** and the task is done. 0901's feasibility verdict is **go**, recorded as the closing section of `docs/analysis/lucene-4-7-codec.md` with a per-module line-count estimate against the thousand-line limit. 0903 has since landed at **473 lines**, 0904 at **854** against a ~800 estimate, 0905 at **740** against ~700, 0907 at **945** against a ~600 estimate — the largest module in the plan and the tightest fit against the gate, where the transducer was expected to be — and 0910's `inverted.rs` at **848**, which §11 gave no estimate. All are inside the thousand-line limit, so no recorded split seam has been needed. Task 0905 also found and fixed a defect in 0903: froe's transducer omitted the one-byte pad Lucene's `FST` constructor writes, which left the empty-key-only transducer — the shape every field of fewer than 49 terms produces — unreadable by Lucene. It landed as `4145766`, and §10.4 of the specification now records the pad rather than the shape. The tightest was expected to be the transducer serializer at roughly 900 lines, which fits partly because froe emits **linear arcs only** — Lucene's reader dispatches on the arc form per node, so that is a valid subset rather than a concession — and the split seam, at the builder/serializer boundary, is recorded in advance so task 0903 plans it rather than discovering it. Merging and deletions are out of scope and neither is needed: froe writes one segment and never merges, and a deletions file is a file the commit references rather than one froe writes. (Coordinator-owned blocked/dropped reasons are recorded here.)

- **Freeze (2026-09-15).** The gate is **closed**: the range is frozen, its review is recorded in `ARCHITECTURE.md`, and every finding is either fixed in a follow-up commit or recorded as a known gap with its reachability stated. The commands and their exit statuses are in plan 0009's verification report, which is the one that ran them for the cumulative tree. The **beta framing stays** on the `index` command family for `v0.12.0`, which is the release that first ships it: the frozen review is this release's own, and four of its findings were defects in shipped code. That is a labelling decision the next release can revisit with a second run of the same evidence behind it, not an open gate.
- **Outcome:** A dependency-free Rust writer for the Lucene 4.7.2 index format Oak selects as `oakCodec`, producing single-segment indexes that Lucene's own index checker accepts and reads back identically to indexes Lucene builds from the same documents.

_Last updated: 2026-09-15, at the `v0.12.0` release candidate._
