# Plan 0008 — Lucene Index Transport — 🚧 In progress

The roll-up row in [../STATUS.md](../STATUS.md) must stay in sync with this file. Task-level truth lives in [tasks/](tasks/) frontmatter; Makina's integration coordinator updates both layers.

- **Status:** 🚧 In progress.
- **Goal:** `froe index dump` and `froe index import` move Lucene index data between the repository and oak-run's filesystem layouts without a JVM, `froe index check` reads segment-level structure, and an index built out of band by Oak's own editors installs into a stopped store that Oak then boots and queries through.
- **Root cause:** oak-run's `--index-import` is the step that touches the production store, and it is pure segment-store work that today still needs the whole Oak runtime; froe has no Lucene file readers at all.
- **Approach:** readers for `segments.gen`, `segments_N`, `.si` and compound files from the vendored Lucene sources; a streaming `:data` writer in the encoding Oak 1.90.0 writes; a state-identity rule in place of oak-run's live bring-up-to-date; the compaction discipline for lock, plan, verify and publish; interop phases whose oracles are Oak's own dumper (byte identity), Lucene's own index checker, Oak's own index consistency check at level 2 and a live Oak answering fulltext queries through the imported index.
- **Progress:** 10/11 tasks done; 0 blocked; 0 dropped.
- **Integration:** `planned`; run —; base `develop` @ `314b9c704fef73636d40f3e7ec5ff2c839aa1870` plus plans 0006 and 0007 merged; validation base —; mode —; final integration —.
- **Exceptions:** 0811 is `gated: true` and its gate is **open**, recorded exactly as 0714's is. Its review work is done and committed — four adversarial lenses (three found real defects, all fixed in the range), the verification report with the five separations, the interoperability record for both `lucene_dump` and `lucene_import`, and the known gaps — but the freeze itself, and lifting the beta framing from `docs/index.md` §6 and §7 and the feature map's rows, is the maintainer's decision. Those rows tell a reader the two commands are ready for a production store, and the evidence behind them has three recorded gaps: no MSRV gate (no 1.89 toolchain on this host), no i686 width sentinel (the attempt fails in `zstd-sys` before any froe code compiles, so it is a gap and not a pass), and no single uninterrupted run of the whole interop chain. The interoperability record itself is complete and every oracle passed. 0810's phase wiring is complete and its five lists — `interop_full`, the chain diagram in `docs/interop.md`, `main.rs`'s numbered module doc, and the script's accept pattern, usage text and header comment — name the same phases in the same order, checked mechanically. Both new phases run alone and pass. **The whole chain does not complete in one run on this host**: the memory watchdog kills it during `property_reindex`'s 23-definition digest loop, which is the same watchdog plans 0006 and 0007 recorded, and it keys on free memory while the page cache holds it near zero with 110+ GB available. Every phase from `generate` through `lucene_import` has been run and passed individually against a freshly generated fixture. Running the chain end to end is what first exposed three defects in plan 0007's reindex and one in `froe index list`, all fixed in `3de6338` and `b80db37` and recorded in plan 0007's own STATUS; that is the work the chain existed to do, and it did it. (Coordinator-owned blocked/dropped reasons are recorded here.)
- **Outcome:** froe reads Lucene index data out of `:data` (`froe index dump`, byte-identical to Oak's dumper), checks it, and imports an out-of-band index built by Oak's own editors back into a stopped store (`froe index import`) so that Oak boots, queries through it, and logs no reindex.

_Last updated: 2026-09-14, against `develop` @ `370000e`._
